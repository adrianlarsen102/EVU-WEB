# ==================================================================================
# EVU-WEB Environment Variables Template
# ==================================================================================
# Copy this file to .env.local for development
# Add actual values to .env.local (never commit .env.local to git!)
#
# For production (Vercel), add these in Project Settings > Environment Variables
# ==================================================================================

# -----------------------------------------------------------------------------------
# DATABASE - Supabase Configuration (REQUIRED)
# -----------------------------------------------------------------------------------
# Get these from your Supabase project: https://app.supabase.com/project/_/settings/api

# Public Supabase URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co

# Service Role Key (KEEP SECRET - server-side only, has full database access)
# Never expose this to the client!
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxx

# -----------------------------------------------------------------------------------
# SECURITY - Encryption & CSRF Protection (REQUIRED)
# -----------------------------------------------------------------------------------

# CSRF Token Secret (minimum 32 characters, random hex string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Used for signing CSRF tokens to prevent cross-site request forgery attacks
CSRF_SECRET=your-random-secret-here-min-32-chars

# Encryption Key for Sensitive Data (e.g., SMTP passwords, API keys)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Used by lib/encryption.js to encrypt/decrypt sensitive data before database storage
ENCRYPTION_KEY=your-encryption-key-here-64-hex-chars

# -----------------------------------------------------------------------------------
# ENVIRONMENT (REQUIRED)
# -----------------------------------------------------------------------------------

# Environment name: development, production, or staging
NODE_ENV=development

# Base URL for the application (used in emails, password reset links, etc.)
# Development:  http://localhost:3000
# Production:   https://yourdomain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------------
# EMAIL CONFIGURATION (OPTIONAL - Can be configured via Admin Panel)
# -----------------------------------------------------------------------------------
# Email settings can be managed in the admin panel (/admin > Email Settings tab)
# These environment variables serve as fallback if database settings are not configured

# Email Provider: 'resend' or 'smtp'
EMAIL_PROVIDER=resend

# Resend API Key (if using Resend)
# Sign up at https://resend.com for free tier (3,000 emails/month)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# SMTP Configuration (if using SMTP instead of Resend)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
SMTP_SECURE=false

# Email sender address and admin email
EMAIL_FROM=noreply@yourdomain.com
ADMIN_EMAIL=admin@yourdomain.com

# -----------------------------------------------------------------------------------
# DISCORD NOTIFICATIONS (OPTIONAL - Can be configured via Admin Panel)
# -----------------------------------------------------------------------------------

# Discord webhook URL for notifications (configure in admin panel or set here as fallback)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/xxxxx

# -----------------------------------------------------------------------------------
# VERCEL DEPLOYMENT (OPTIONAL - Auto-configured on Vercel)
# -----------------------------------------------------------------------------------

# Vercel Cron Secret (for scheduled jobs like metrics collection)
# Auto-generated by Vercel, no need to set manually
# CRON_SECRET=xxxxx

# -----------------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------------
# 1. Never commit .env.local or .env files to version control
# 2. In production (Vercel), set these in Project Settings > Environment Variables
# 3. Required variables: SUPABASE_*, CSRF_SECRET, ENCRYPTION_KEY, NODE_ENV
# 4. Generate secure random secrets using crypto.randomBytes()
# 5. Email and Discord settings can be managed via admin panel (/admin)
# 6. Restart dev server after changing environment variables
# -----------------------------------------------------------------------------------
