# ====================================
# REQUIRED CONFIGURATION
# ====================================

# Supabase Configuration
# Get these values from your Supabase project settings
# https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Your Supabase service role key (keep this secret!)
# This is the service_role key, NOT the anon key
# Find it in: Project Settings > API > Project API keys > service_role
SUPABASE_SERVICE_ROLE_KEY=

# Supabase Database Password (for automatic SQL migrations)
# OPTIONAL: Only needed if you want to use `npm run db:push` for automatic migrations
# Find it in: Project Settings > Database > Connection Info > Password
# Or use the password you set when creating your Supabase project
SUPABASE_DB_PASSWORD=

# Security Configuration
# REQUIRED: CSRF token signing secret (must be at least 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CSRF_SECRET=

# ====================================
# OPTIONAL CONFIGURATION
# ====================================

# Environment Mode
# Set to 'production' in production, 'development' for local dev, 'test' for testing
NODE_ENV=development

# Application URLs
# Used for CORS, redirects, and email links
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ====================================
# EMAIL CONFIGURATION (Optional)
# Only needed if you want to send emails (support tickets, notifications, etc.)
# ====================================

# Email Provider: 'resend' or 'smtp'
# Leave empty to disable email functionality
EMAIL_PROVIDER=

# Resend Configuration (if using Resend)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=

# SMTP Configuration (if using SMTP like Gmail, Outlook, etc.)
# Gmail example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_SECURE=false
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-password
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Email "From" Configuration
# The email address and name that emails will be sent from
EMAIL_FROM=noreply@yourdomain.com
ADMIN_EMAIL=admin@yourdomain.com

# ====================================
# DISCORD WEBHOOK CONFIGURATION (Optional)
# Only needed if you want Discord notifications
# ====================================

# Discord Webhook URL
# Get from: Discord Server > Edit Channel > Integrations > Webhooks
DISCORD_WEBHOOK_URL=

# Discord Bot Avatar (optional)
# URL to an image for the webhook bot avatar
DISCORD_BOT_AVATAR_URL=

# ====================================
# VERCEL CONFIGURATION (Optional)
# Only needed for Vercel Cron jobs
# ====================================

# Cron Job Secret
# Used to authenticate Vercel Cron job requests
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=
